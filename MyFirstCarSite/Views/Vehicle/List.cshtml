@model VehiclesListViewModel

@foreach (var v in Model.Vehicles)
{
    @Html.Partial("VehicleSummary", v)
}

<div page-model="@Model.PagingInfo" page-action="List" page-classes-enabled="true" page-class="btn" page-class-normal="btn-secondary" page-class-selected="btn-primary" page-url-make="@Model.CurrentCategory" class="btn-group pull-right m-1">
</div>

    @*<div class="card card-outline-primary m-1 p-1">
        <div class="bg-faded p-1">
        <h4>
            @v.Make
            <span class="badge badge-pill badge-primary" styles="float:right">
                <small>@v.Price.ToString("c")</small>
            </span>
        </h4>
    </div>
        <div class="card-text p-1">@v.VehicleModel</div>
    </div>*@


